import React from 'react'; import { ChevronDown } from 'lucide-react'; import FormField from './FormField.jsx';
export default function SelectInput({id,label,icon,helper,error,success,required,register,options,placeholder='Select an option'}){return <FormField label={label} htmlFor={id} icon={icon} helper={helper} error={error} success={success} required={required}><div className="input-shell select-shell">{icon&&React.createElement(icon,{size:17})}<select id={id} {...register}><option value="">{placeholder}</option>{options.map(o=><option key={o} value={o}>{o}</option>)}</select><ChevronDown size={16} className="select-chevron"/></div></FormField>}
