import React,{useState} from 'react'; import { Eye,EyeOff,KeyRound } from 'lucide-react'; import FormField from './FormField.jsx';
export default function PasswordInput({id,label,helper,error,success,required,register}){const [visible,setVisible]=useState(false);return <FormField label={label} htmlFor={id} helper={helper} error={error} success={success} required={required} icon={KeyRound}><div className="input-shell"><KeyRound size={17}/><input id={id} type={visible?'text':'password'} placeholder="Create secure password" {...register}/><button type="button" className="inline-action" onClick={()=>setVisible(v=>!v)}>{visible?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></FormField>}
