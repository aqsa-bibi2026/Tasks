const express=require('express');const cors=require('cors');const app=express();app.use(cors());app.use(express.json());app.get('/',(req,res)=>res.json({success:true,project:'MedCare Enterprise API'}));app.post('/api/patients',(req,res)=>res.json({success:true,message:'Patient created successfully',data:req.body}));app.listen(5000,()=>console.log('MedCare API running on port 5000'));