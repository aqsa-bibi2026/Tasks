import React,{useState} from 'react';import {ArrowRight,Boxes,Eye,EyeOff,Layers3,LoaderCircle,LockKeyhole,Mail,Network,Route} from 'lucide-react';import {api} from './api.js';
export default function Login({onSuccess}){const[email,setEmail]=useState('admin@layerdesk.dev');const[password,setPassword]=useState('Admin@12345');const[show,setShow]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');async function submit(e){e.preventDefault();setLoading(true);setError('');try{onSuccess((await api.login({email,password})).user)}catch(e){setError(e.message)}finally{setLoading(false)}}return <div className="login-page"><section className="login-showcase"><div className="showcase-grid"/><div className="brand login-brand"><span><Layers3/></span><div><b>LayerDesk</b><small>Service Operations Platform</small></div></div><div className="showcase-content"><span className="eyebrow">TASK 31 / CLEAN BACKEND ARCHITECTURE</span><h1>Business logic,<em> built in the right layer.</em></h1><p>A production-style service operations dashboard backed by controllers, services, repositories and Supabase.</p><div className="architecture-preview">{[[Route,'Routes','Endpoints + middleware'],[Network,'Controllers','HTTP input + output'],[Boxes,'Services','Business rules'],[Layers3,'Repositories','Supabase access']].map(([I,t,s],i)=><React.Fragment key={t}><article><span><I/></span><b>{t}</b><small>{s}</small></article>{i<3&&<i/>}</React.Fragment>)}</div></div></section><section className="login-panel"><form className="login-card" onSubmit={submit}><span className="eyebrow">SECURE ADMIN ACCESS</span><h2>Welcome to LayerDesk</h2><p>Sign in to open the service operations workspace.</p>{error&&<div className="form-error">{error}</div>}<label><span><Mail/> Email address</span><div className="input-shell"><Mail/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div></label><label><span><LockKeyhole/> Password</span><div className="input-shell"><LockKeyhole/><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(v=>!v)}>{show?<EyeOff/>:<Eye/>}</button></div></label><button className="primary login-submit" disabled={loading}>{loading?<><LoaderCircle className="spin"/> Signing in...</>:<>Open operations <ArrowRight/></>}</button><div className="demo-box"><span>Demo administrator</span><code>admin@layerdesk.dev</code><code>Admin@12345</code></div></form></section></div>}
